#include <iostream>
#include <vector>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    
    vector<int> vec;
    
    vec.push_back(10);
    vec.push_back(20);
    vec.push_back(30);
    vec.push_back(40);
    vec.push_back(60);
    vec.push_back(90);

    for(int val : vec)
    {
         cout<< vec.
    }

    cout<<endl;
    
    vector<int> vec2(5,-999);
     
    cout<<endl;

    vector<int> vec3 = {1,2,3,4,5};

    cout<<endl;

    vector<int>vec1(vec);


    return 0;
}